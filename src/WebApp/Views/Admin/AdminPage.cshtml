@{
    ViewBag.Title = "AdminPage";
}


<div>
    <input type="text" name="form-control" placeholder=""/>
    <button>Filter</button>
</div>

<div>
    <div>Stammdaten Übersicht</div>
    <div>Verwalten Sie hier alle Stammdaten der Anwendung</div>
   
    
    <form>
        <table>
            <tbody>
        <tr>
            <thead>
            <th><input type="checkbox" /></th>
            <th>ID</th>
            <th>NAME</th>
            @*@if (Model.){

                <th>KATEGORIE</th>
                <th>STATUS</th>
                <th>STARTDATUM</th>
                <th>ENDDATUM</th>
                }
                *@
            <th>AKTIONEN</th>
    
            </thead>
            </tr>
            <tbody>
            @*@foreach (var item in Model.)
                    {
                    <tr>
                        <td> <input type = "checkbox" checked="@(item.Checked)"/></td>
                        <td>@item.Id</td>
                        <td>
                            <div>
                                <div>@item.Name</div>
                                @if(!string.IsNullOrEmpty(item.Beschreibung))
                                {
                                <div>@item.Beschreibung</div>
                                }
                                </div>
                                </td>
                                @if (Model.ShowstatusColumn)
                                {
                                <td>@item.Kategroie</td>
                                <td>
                                    @if (!string.IsNullOrEmty(item.Status))
                                    {
                                    <span>@item.Status </span>

                                    }
                                    </td>
                                    <td>@item.Start</td>
                                    <td>@item.End</td>
                                }*@
            <td>
            <a href="#" >Bearbeiten</a>
            @*@if(Model.)
                                    {
                                    <a href="#"> Löschen</a>
                                    }
                                    </td>
                                    </tr>
                    }*@ 
              
            </tbody>
            
</table>
    </form>
</div>

<!--Anzeigeleiste von Ergebnissen-->
<div class="d-flex align-items-center justify-content-between mt-3 flex-wrap">
    <div class="small text-secondary">
        Zeige 1 bis 10 von @*@Model.ResultCount*@ Ergebnissen
    </div>
  
        <a class="page-link" href="#">Zurück</a></li>
            @*@for (int i = 1; i <= Model.PageCount; i++)
            {
                <li class="page-item @(i == Model.Page ? "active" : "")">
                    <a class="page-link" href="#">@i</a>
                </li>
            }*@
           <a class="page-link" href="#">Weiter</a></li>
    
   
      
    <div class="small text-secondary">
        @*@Model.*@ Einträge ausgewählt
    </div>
</div>

        <div>
            <input type="text" class="form-control"/>
            <button>Anwenden</button>
        </div>